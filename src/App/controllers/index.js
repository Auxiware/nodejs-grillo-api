// controllers exportation
module.exports = {
    User: require('./UserController').UserController,
    Session: require('./SessionController').SessionController,
    Post: require('./PostController').PostController
}